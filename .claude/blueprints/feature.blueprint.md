---
id: feature.blueprint
type: blueprint
version: 1.0.0
created: 20/03/2026
modified: 20/03/2026
status: active
toolshed: "auto-detect # frontend if react/ui/component keywords, backend if api/fastapi/endpoint keywords"
---


# Feature Blueprint

> One-shot DAG for implementing a new feature from task description to PR.

## DAG

```
git-branch
  → implement(1)
  → lint
  → [fix-lint(1)]
  → type-check
  → test
  → [fix-ci(2)]
  → git-commit
  → git-push
  → create-pr
```

## Toolshed Detection

Scan task text for keywords:

- `react|component|ui|tailwind|nextjs|page|layout` → toolshed: `frontend`
- `api|fastapi|endpoint|route|python|pydantic` → toolshed: `backend`
- `database|migration|sql|schema|postgres|alembic` → toolshed: `database`
- Default: `general`

## Pre-Hydration Context (loaded before `implement` node)

Always loaded:

- `.claude/memory/CONSTITUTION.md`
- `.claude/memory/current-state.md`

Frontend domain:

- `apps/web/lib/design-tokens.ts`
- `apps/web/app/layout.tsx`
- `.skills/custom/scientific-luxury/SKILL.md` (relevant sections only)

Backend domain:

- `apps/backend/src/api/main.py`
- `apps/backend/src/config/database.py`
- `apps/backend/src/auth/jwt.py`

## Iteration Caps

| Node        | Cap   |
| ----------- | ----- |
| `implement` | 1     |
| `fix-lint`  | 1     |
| `fix-ci`    | 2     |
| **Total**   | **3** |

## PR Template

```markdown
## Summary

{1-3 bullet points describing what was implemented}

## Blueprint Metadata

- **Type**: Feature
- **Task ID**: {task_id}
- **Blueprint**: feature v1.0.0
- **Toolshed**: {toolshed}
- **Agentic iterations used**: {n}/3
- **Generated**: {DD/MM/YYYY} AEST

## Scientific Luxury Checklist

- [ ] OLED Black background `#050505` used (no pure white backgrounds)
- [ ] `rounded-sm` corners (no `rounded-lg` or `rounded-full`)
- [ ] Framer Motion only (no CSS transitions)
- [ ] Spectral colours: cyan `#00F5FF`, emerald `#00FF88`, amber `#FFB800`
- [ ] JetBrains Mono for data, Editorial New for headings
- [ ] en-AU locale: colour, behaviour, optimisation (not color, behavior, optimization)

## Test Plan

- [ ] Type check passes (`pnpm turbo run type-check`)
- [ ] Lint passes (`pnpm turbo run lint`)
- [ ] Unit tests pass (`pnpm turbo run test`)
- [ ] Manual smoke test performed

---

Generated by Minion Blueprint Engine (feature v1.0.0)
```

## Escalation Conditions

- `implement` node fails or produces no changes → immediate escalation (no retry)
- `fix-ci` reaches 2 rounds with failures still present → escalation
- Any node produces a security-relevant change (auth, CORS, JWT) → escalation for human review
